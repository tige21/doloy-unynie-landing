# Fix Plan: Сократить длину скролла и сделать блок звука понятным

**Problem:** (1) Сайт слишком долго скроллить — огромные расстояния между блоками; (2) непонятен одинокий блок «Тут лучше со звуком, честно» — пользователь не понял, что это и зачем.
**Created:** 2026-07-13 14:05

## Analysis

Замер фактической высоты (десктоп 1440×900, прод-сборка): **22 820px = 25,4 экрана**.

Разбивка по виновникам:

| Источник | Сейчас | Вклад |
|---|---|---|
| Pin-спейсеры (исповедь 2.5vh, дверь 1.8vh, крик 1.8vh) | ~5 490px | главный |
| scene-8 «Твоё воскресенье» | 2 828px | отступы блоков 6 тактов, крупные фото |
| scene-3 «Ты в команде» | 2 798px | отступы turn/jab/rules по 5–6 тактов |
| scene-7 «Автор» | 2 078px | dialog gap 3 такта, паддинги 8 тактов |
| scene-6 «Выдох» | 1 662px | паддинги 8 тактов |
| scene-4b «Вдох» | 900px (100svh) | пустой экран под одну строку |
| scene-9 «Титр» | 933px | min-height 70svh + gap 3 такта |

Дополнительно: Lenis `duration: 1.1` субъективно замедляет ход.

Блок звука: сцена 4½ — одинокая строка с самодельной SVG-иконкой, без контекста «зачем» и без явного действия. Пользователь не понимает, что дальше будет видео с криком и что это переключатель.

## Fix Steps

1. [ ] Сократить pin-длины: `prologue.ts` исповедь 2.5→1.4; `door.ts` 1.8→1.1; `scream.ts` 1.8→1.1 (тайминги внутри таймлайнов не трогать — они в относительных единицах scrub)
2. [ ] Ужать вертикаль сцен: scene-3 (turn 6→3 тактов, jab 5→2, rules-wrap 5→2.5), scene-6/7 (padding-block 8→4, s7-dialog gap 3→1.5, margin 6→3), scene-8 (steps/go margin 6→3, s8-door max-inline 26→20rem), scene-2 (s2-small margin 4→2), scene-4 (row-gap 1.5→1)
3. [ ] Пустые экраны: scene-4b min-height 100svh→60svh, padding 12→4 тактов; scene-9 min-height 70svh→50svh
4. [ ] Lenis duration 1.1→0.85 (бодрее ход)
5. [ ] Переделать блок звука (index.html + scene4.css + sound-consent.ts не трогается — API тот же):
   - заголовок-реплика: «Сейчас будет громко.»
   - пояснение голосом: «Дальше — то, ради чего всё: тридцать человек хором кричат. Лучше со звуком, честно.»
   - явная кнопка-переключатель с состояниями: «Включить звук» → «Звук включён ✓» (заметный фон, а не голая строка)
6. [ ] **[юзабилити-прогон] Контраст рассвета:** в момент night→day чернила переключаются мгновенно, а фон шейдера лерпится 1,8 с — третья мысль исповеди становится серой на тёмном (нечитаемо, walk-d02). Фикс: `transition: color 1.8s ease` на `.s1-thought` (+ `stamp` сцены 2), синхронно шейдеру; reduce-motion — без transition
7. [ ] **[юзабилити-прогон] Грязный кросс-фейд зума сцены 2:** в середине перехода фрагмент и толпа накладываются полупрозрачной «двойной экспозицией» (walk-d03). Фикс: последовательность вместо наложения — фрагмент полностью уходит (scale+fade до 0) на 0.5–1.4, толпа появляется с 1.5 (без окна перекрытия)
8. [ ] **[юзабилити-прогон] Артефакт «зависшая волна»:** после крика посреди экрана остаётся оранжевая клякса — фронт волны застревает в промежуточном значении (walk-d13). Расследовать: добавить `[FIX]`-лог uWave/waveStart в цикле атмосферы; fail-safe в шейдере/цикле — band жёстко 0, если с момента strike прошло > 1,2 с (гарантированное завершение)
9. [ ] **[юзабилити-прогон] Пустые стыки после крика:** сцена 6→7 (полэкрана под мемом-котом, walk-d10) и сцена 7→8 (полтора пустых экрана на мобиле, walk-m2) — входит в шаг 2, проверить именно стыки; диалог сцены 7 прижат влево с пустой правой колонкой — центрировать `.s7-dialog` (margin-inline: auto)
10. [ ] Логирование `[FIX]`: замер высоты страницы в debug-режиме (dbg 'scroll' page height) + лог смены состояния кнопки звука + лог uWave
11. [ ] Верификация: повторный замер — целевая высота ≤ ~17 000px десктоп (≈19 экранов, −25%) и ≤ ~15 500px мобайл; скриншоты блока звука, стыков 6/7/8 и рассветного кадра исповеди; прогон кульминации (удары живы, клякса не воспроизводится)

## Files to Modify

- `src/lib/scenes/prologue.ts` — pin 2.5→1.4
- `src/lib/scenes/door.ts` — pin 1.8→1.1
- `src/lib/scenes/scream.ts` — pin 1.8→1.1
- `src/lib/motion.ts` — Lenis duration 0.85, лог высоты страницы
- `src/styles/scenes/scene2.css, scene3.css, scene4.css, scene6.css, scene7.css, scene8.css` — отступы по шагам 2–3
- `index.html` — блок звука сцены 4½ (заголовок + пояснение + кнопка)
- `src/styles/scenes/scene4.css` — стили нового блока звука
- `src/styles/scenes/scene1.css` — transition цвета мысли (рассвет)
- `src/lib/scenes/door.ts` — тайминги кросс-фейда (последовательность)
- `src/lib/webgl/atmosphere.ts` — fail-safe волны + [FIX]-лог uWave
- `src/styles/scenes/scene7.css` — центрирование диалога

## Risks & Considerations

- Короткие pin-длины ускоряют смену мыслей исповеди — проверить, что тексты успевают читаться (scrub 0.6 сглаживает)
- Тайминг `.call()` в таймлайнах задан в абсолютных юнитах длительности таймлайна — они НЕ зависят от pin-длины (длительность таймлайна прежняя, меняется только маппинг на пиксели скролла) — но перепроверить рассвет и старт крика
- Сцена 4½ короче — пауза перед криком остаётся отдельным экраном (60svh), драматургия сохранена
- Не трогать механику ударов/звука (climax.ts)

## Test Coverage

- Тестов в проекте нет (решение заказчика); верификация — инструментальная: замер scrollHeight до/после, прогон кульминации по debug-логам, скриншоты блока звука (десктоп/мобайл)
